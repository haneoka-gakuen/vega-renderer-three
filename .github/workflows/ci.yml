name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: vega-renderer-three-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out Three renderer
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: vega-renderer-three
          persist-credentials: false

      - name: Check out Vega
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: haneoka-gakuen/vega
          path: vega-renderer-three/.dependencies/vega
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 11.14.0

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: vega-renderer-three/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: vega-renderer-three
        run: pnpm install --no-frozen-lockfile

      - name: Build Vega protocol
        working-directory: vega-renderer-three
        run: pnpm --filter @haneoka/vega-protocol build

      - name: Build Vega renderer kit
        working-directory: vega-renderer-three
        run: pnpm --filter @haneoka/vega build:core

      - name: Check package
        working-directory: vega-renderer-three
        run: pnpm check

      - name: Inspect npm payload
        working-directory: vega-renderer-three
        run: pnpm pack --dry-run
